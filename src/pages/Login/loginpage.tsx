import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  typography: {
    h1: {
      color: "var(--Vigo-Dark-Blue, #2B2D42)",
      fontFamily: "Quicksand, sans-serif",
      fontSize: "32px",
      fontWeight: 500,
      letterSpacing: "0.013px",
    },
  },
});
